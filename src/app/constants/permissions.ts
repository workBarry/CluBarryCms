import { UserRole } from '../types/admin.models';

export const roleOrder: UserRole[] = ['Member', 'Activity Leader', 'Vice President', 'Admin'];

export const permissionModules = ['Dashboard', '公告', '活動', '社員', '報名', '系統'];
